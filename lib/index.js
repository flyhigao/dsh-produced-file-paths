/**
 * dsh-produced-file-paths host half.
 *
 * The feature is browser-only. The host half exists solely so the package can
 * be composed as an independent DSH bundle and contribute its client entry.
 */
export const name = 'dsh-produced-file-paths';

export const inject = [];

export function apply(_ctx, _config = {}) {}
